#include<iostream>
#include<vector>
#include<
using namespace std;
class Solution {
public:
    bool canPartition(vector<int>& nums) {
        sort
        int head=0;
        int tail=nums.size()-1;
        int first_sum=0;
        int last_sum=0;
        while(tail>head){
            if(first_sum<)
        }
    }
};